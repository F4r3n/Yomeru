[package]
name = "examples-types"
version = "0.8.2"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }

[lints]
workspace = true
